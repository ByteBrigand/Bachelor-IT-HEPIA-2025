--- Makefile.old
+++ Makefile.new
@@ -1,8 +1,11 @@
 ifeq ($(ARCH),x86_64)
   ARCH_TYPE=-m64
   OSDIR=linux_x86_64
+else ifeq ($(ARCH),aarch64)
+  ARCH_TYPE=
+  OSDIR=linux_arm_64
 else
   ARCH_TYPE=-m32
   OSDIR=linux_x86_32
 endif